import { DiretivaCorDirective } from './diretiva-cor.directive';

describe('DiretivaCorDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaCorDirective();
    expect(directive).toBeTruthy();
  });
});
